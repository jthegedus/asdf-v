#!/usr/bin/env bash

set -euo pipefail

current_script_path=${BASH_SOURCE[0]}
plugin_dir=$(dirname "$(dirname "$current_script_path")")

# shellcheck source=../lib/latest-stable.bash
. "${plugin_dir}/lib/latest-stable.bash"

latest_stable_version
