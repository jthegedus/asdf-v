#!/usr/bin/env bash

set -euo pipefail

current_script_path=${BASH_SOURCE[0]}
plugin_dir=$(dirname "$(dirname "$current_script_path")")

# shellcheck source=../lib/list-all.bash
. "${plugin_dir}/lib/list-all.bash"

list_all_versions | sort_versions | xargs echo # asdf_allow: echo
